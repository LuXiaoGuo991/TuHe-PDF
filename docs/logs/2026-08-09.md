---
date: 2026-08-09
tags: [progress, planning, testing, deployment, logs]
status: active
---

# 2026-08-09

## Progress Review

Recorded: 2026-08-09 10:24:58 +08:00

### Background and Objective

Reviewed the repository logs and current working tree at the user's request to assess project progress and define the next implementation sequence. This was an investigation and planning task; no application code or deployment configuration was changed.

### Prior Log Entries Consulted and Decisions Preserved

- Read `docs/logs/README.md` and `docs/logs/2026-08-08.md` before inspection.
- Preserved AGPL-3.0 `LICENSE`, the `tuhe-pdf` service name, port `8080`, Windows-to-Linux shell line-ending normalization, and the requirement for the exact qpdf PDF fixtures.
- Preserved the prior deployment decision to keep the container non-root and to defer replacing `SITE_URL=http://localhost:8080` until the final HTTPS domain is known.

### Current State Observed

- Repository baseline and local Docker deployment are in place; `main` is one commit ahead of `origin/main`.
- The current compose configuration builds `tuhe-pdf`, exposes `8080:8080`, and still uses provisional `TuHe PDF` branding and the localhost canonical URL.
- The last recorded Docker verification was successful: container up and HTTP 200 on `http://localhost:8080`.
- Quality status remains 841/843 Vitest assertions passing. Two failures are fixture-dependent tests in `src/tests/split-pdf-helpers.test.ts`; only `sample.pdf` is present.
- ESLint completed with warnings only; no lint error blocked the prior check.
- The working tree contains an uncommitted timestamp addition in `docs/logs/2026-08-08.md`; this review did not alter or revert it.

### Recommended Next Steps

1. Restore or generate the exact `shared-resources.pdf` and `bookmarked.pdf` fixtures, then rerun the focused split-PDF tests and the full `npm run test:run` suite.
2. Decide the deployment target and final HTTPS domain; update `SITE_URL`, branding, and any reverse-proxy/TLS settings only after that decision, then rebuild and verify HTTP/HTTPS health.
3. Run a production-quality gate (`npm run build`, `npm run lint`, and an appropriate security audit) and triage warnings that are relevant to the personal deployment.
4. Exercise a small smoke-test matrix in the running container, including representative PDF tools, browser loading, asset paths, and restart behavior.
5. Commit the completed work and keep the daily log/index synchronized; retain the known qpdf fixture and deployment risks until verified.

### Commands and Verification

- `Get-Content -Raw docs/logs/README.md`
- `rg -n "进度|下一步|TODO|未完成|风险|失败|阻塞|decision|决定|feature|error" docs/logs`
- `Get-Content -Raw docs/logs/2026-08-08.md`
- `Get-ChildItem docs/logs -Force`
- `git status --short --branch`, `git diff -- docs/logs/2026-08-08.md`, `git log --oneline --decorate -8`, `git remote -v`
- Inspected `src/tests/fixtures`, `package.json`, `compose.yml`, and `Dockerfile`.

All inspection commands completed. No tests or deployment commands were rerun during this review.

### Known Warnings, Failures, Risks, and Follow-up

- Two qpdf integration tests remain red because required fixtures are missing; do not substitute `sample.pdf`.
- Existing ESLint warnings remain untriaged.
- `SITE_URL` is still localhost and is unsuitable for external HTTPS deployment until the domain is selected.
- The uncommitted change in the previous daily log should be handled deliberately when the next commit is prepared.

## Deployment Decision Intake

Recorded: 2026-08-09 10:35:32 +08:00

### Background and Objective

Started the second planned step: collect beginner-friendly deployment requirements before changing `SITE_URL`, branding, TLS, or reverse-proxy configuration.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md`, `docs/logs/2026-08-08.md`, and the earlier progress-review section in this file.
- Preserved the local `tuhe-pdf`/8080 baseline and the decision to defer the final HTTPS canonical URL until the deployment target is known.

### Files and Changes

- Changed only this task log; no application or deployment configuration was changed.

### Commands and Verification

- Read the logging index, searched deployment terms with `rg`, and inspected `compose.yml`.
- Confirmed `SITE_URL=http://localhost:8080` remains provisional.

### Open Question and Risk

- Awaiting the user's deployment target choice before selecting a domain, DNS, HTTPS certificate, or reverse-proxy setup.

## Deployment Target Clarification

Recorded: 2026-08-09 10:37:27 +08:00

### Background and Objective

The user confirmed a two-stage direction: continue local secondary development on their own computer, then deploy to an already-provisioned and备案-compliant server and domain.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched the 2026-08-08 and 2026-08-09 entries for deployment, Docker, `SITE_URL`, HTTPS, and domain decisions.
- Preserved the local `localhost:8080` development baseline and deferred production URL changes until server runtime details are known.

### Files and Changes

- Changed only this task log; no application or deployment configuration was changed.

### Commands and Verification

- Read the logging index and deployment-related prior entries with `Get-Content` and `rg`.
- Confirmed that the existing `compose.yml` remains a local Docker Compose configuration with `SITE_URL=http://localhost:8080`.

### Follow-up Question

- Need the server operating system and whether Docker/Docker Compose is already installed before choosing the production deployment instructions.

## Server Screenshot Review

Recorded: 2026-08-09 10:39:40 +08:00

### Background and Objective

Reviewed the user's server-management screenshot to answer the deployment-target question without requesting credentials or changing configuration.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched prior deployment entries before interpreting the screenshot.
- Preserved the local development URL and the existing non-root Docker approach.

### Current Information Observed

- Server OS: Alibaba Cloud Linux 3.2104 LTS, 64-bit.
- Allocated capacity: 2 vCPU, 2 GiB memory, and 40 GiB system disk.
- The server has public-network connectivity. No IP address or credential was recorded.
- The 2 GiB memory limit may make server-side `npm ci` and application builds sensitive; local image building or additional swap may be preferable.

### Files and Changes

- Changed only this task log; no application or deployment configuration was changed.

### Commands and Verification

- Read the logging index and searched deployment-related terms with `rg`.
- Verified the screenshot provides the OS and resource information but does not establish Docker installation status.

### Follow-up Question and Risk

- Need Docker and Docker Compose version information before selecting the production installation path.

## ECS Credential Guidance

Recorded: 2026-08-09 10:45:14 +08:00

### Background and Objective

Answered the user's question about an unknown Alibaba Cloud ECS instance password. No credentials were requested, exposed, or recorded.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched the deployment-related entries before providing guidance.
- Preserved the two-stage plan: local development first, server deployment later.

### Guidance Given

- Alibaba Cloud ECS does not provide a viewable default or initial Linux password.
- The user should use the ECS console's Reset Password action to create a new strong password, then connect with Workbench or SSH.
- Depending on Cloud Assistant support, an online reset may apply without reboot; an offline reset requires restarting the instance and can interrupt services.
- The default Linux login is commonly `root`; `ecs-user` may apply if it was selected during instance creation. The user must not share the password or private key.

### Sources and Verification

- Consulted Alibaba Cloud official ECS credential-management documentation and reset-password guidance.
- No repository files other than this log were changed; no server command was run.

### Follow-up

- After the user resets or otherwise obtains access, collect `docker --version` and `docker compose version` output before choosing the production deployment path.

## Docker Installation Guidance

Recorded: 2026-08-09 10:51:25 +08:00

### Background and Objective

The user confirmed that `docker --version` returns `docker: command not found` on the Alibaba Cloud Linux 3 server. Prepared the next installation step; no remote command was executed by the agent.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched all deployment and Docker entries.
- Preserved the existing local Compose service name `tuhe-pdf`, container port `8080`, non-root container runtime, and local `SITE_URL` until production deployment is configured.

### Recommended Commands

- Use the Alibaba Cloud Linux 3 Docker CE repository and compatibility plugin.
- Install Docker Engine, Buildx, and the Docker Compose plugin.
- Enable and start the Docker service, then verify with `hello-world`, `docker --version`, and `docker compose version`.

### Sources and Verification

- Consulted Alibaba Cloud official Docker installation guidance and Docker's official installation/Compose documentation.
- The only observed server result is the missing Docker command; installation and verification remain pending user execution.

### Risks and Follow-up

- Package installation requires server network access and may prompt for repository/GPG confirmation.
- The server has 2 GiB memory; later image builds may be memory-sensitive, so local image builds remain an option.

## Docker Registry Connectivity Check

Recorded: 2026-08-09 10:56:16 +08:00

### Background and Objective

Reviewed the user's Docker installation verification. Docker Engine 26.1.3 and Docker Compose v2.27.0 are installed, enabled, and available; the `hello-world` test failed while waiting for Docker Hub's registry response.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched the deployment/Docker entries.
- Preserved the 2 GiB server-memory constraint and the option to keep server-side runtime separate from local image building.

### Current State

- Installation and service startup succeeded.
- Registry access to `registry-1.docker.io` timed out; this is a network/registry-path issue, not evidence of a broken Docker daemon.

### Recommended Direction

- Prefer local image building followed by image transfer or a private registry push, especially while the server has 2 GiB memory.
- An Alibaba Cloud ACR mirror is an alternative, but it requires an account-specific accelerator address and has documented freshness/usage limitations.

### Files and Changes

- Changed only this task log; no application or deployment configuration was changed.

### Commands and Verification

- Reviewed the user-provided output for `systemctl enable --now docker`, `docker --version`, `docker compose version`, and `docker run --rm hello-world`.
- Consulted Alibaba Cloud official ACR mirror guidance and Docker official installation documentation.

### Follow-up Question

- Need to know whether the user has an Alibaba Cloud Container Registry (ACR) instance or wants to use local image transfer instead.

## Xuanyuan Mirror Option

Recorded: 2026-08-09 11:01:35 +08:00

### Background and Objective

The user asked whether the server can use Xuanyuan Mirror after Docker Hub access timed out. Evaluated the mirror as a Docker registry accelerator; no server or repository configuration was changed by the agent.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched prior Docker, registry, and deployment entries.
- Preserved the local development baseline and the plan to avoid unnecessary server-side builds under the 2 GiB memory limit.

### Technical Assessment

- Xuanyuan documents `https://docker.xuanyuan.me` as a public Docker Hub mirror endpoint and shows configuration through `/etc/docker/daemon.json` with `registry-mirrors`.
- A mirror accelerates pulls of public Docker Hub images; it is not the same as a private registry for storing the TuHe application image.
- Treat the public/free endpoint as suitable for initial development/testing only; production should use a dedicated/private registry or account-specific service with an understood availability and usage policy.

### Files and Changes

- Changed only this task log; no application code, repository configuration, or server configuration was changed.

### Sources and Verification

- Consulted the Xuanyuan Mirror configuration documentation and Alibaba Cloud ACR mirror guidance.
- The prior Docker error remains a timeout from `registry-1.docker.io`; no retry was run.

### Follow-up

- The user can configure the public endpoint for a controlled test, or provide their account-specific Xuanyuan endpoint if they have one.

## Xuanyuan Dedicated Endpoint Configured

Recorded: 2026-08-09 11:18:28 +08:00

### Background and Objective

The user reported running the Xuanyuan installation/configuration script and subscribing to a paid Xuanyuan endpoint. The reported Docker registry endpoint is `docker.xuanyuan.run`.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched all prior Xuanyuan, registry, Docker, and timeout entries.
- Preserved the existing Docker installation and the requirement to verify registry configuration before pulling or building the application.

### Current State and Follow-up

- Treat the server-side registry setup as user-applied and unverified until `docker info` and a test pull succeed.
- Do not request or record Xuanyuan account passwords, access tokens, or private credentials.
- Next verification should check the configured mirror and rerun `docker run --rm hello-world`.

### Files and Changes

- Changed only this task log; no application or repository configuration was changed.

## Xuanyuan Pull Fallback Diagnosis

Recorded: 2026-08-09 11:21:39 +08:00

### Background and Objective

Reviewed the user's verification output after the paid Xuanyuan endpoints appeared under `docker info` but `docker run hello-world` still timed out against `registry-1.docker.io`.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read `docs/logs/README.md` and searched all prior registry, Xuanyuan, Docker, and timeout entries.
- Preserved the configured mirror endpoints and the rule not to request or record credentials.

### Diagnosis

- Mirror configuration is loaded by Docker.
- Docker may fall back to Docker Hub when a mirror request does not complete; the official Xuanyuan guidance recommends explicit image-domain pulls for reliable routing.
- The paid endpoint likely also requires a separate `docker login` using credentials shown in the Xuanyuan account's image-repository information.

### Follow-up Commands

- Ask the user to run interactive `docker login docker.xuanyuan.run`, then explicitly pull and run `docker.xuanyuan.run/library/hello-world:latest`.
- Do not ask the user to paste credentials; only the success or sanitized error result is needed.

### Files and Changes

- Changed only this task log; no application or server configuration was changed by the agent.

### Sources and Verification

- Consulted Xuanyuan official login, Compose, and registry-mirror troubleshooting documentation.
- No remote command was executed by the agent; the user's prior `docker info` and `docker run` outputs were the evidence.

## Docker Registry Verification Passed

Recorded: 2026-08-09 11:30:20 +08:00

### Background and Objective

Verified the user's explicit pull/run test using the Xuanyuan registry endpoint.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched prior Docker, registry, and deployment entries.
- Preserved explicit Xuanyuan-domain pulls as the reliable path when Docker Hub fallback is unavailable.

### Verification Result

- `docker run --rm docker.xuanyuan.run/library/hello-world:latest` completed successfully and printed `Hello from Docker!`.
- Docker installation, Compose availability, registry authentication/routing, and container execution are now confirmed.

### Files and Changes

- Changed only this task log; no application or repository configuration was changed.

### Follow-up

- Move to application delivery: determine whether the domain DNS record already points to the server, then choose local image transfer versus server-side build.

## DNS Setup Pending

Recorded: 2026-08-09 11:39:59 +08:00

### Background and Objective

The user confirmed that the production domain does not yet resolve to the server. Began DNS planning before any `SITE_URL`, HTTPS, or reverse-proxy changes.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched domain, DNS, HTTPS, deployment, and `SITE_URL` entries.
- Preserved the local `localhost:8080` build configuration until the final public hostname is selected and resolves correctly.

### Current State

- Server container prerequisites and authenticated image pulls are verified.
- DNS, the final public hostname, HTTPS certificate issuance, and public application deployment remain pending.

### Follow-up Questions

- Need to select whether the application uses the apex domain, `www`, or a dedicated subdomain such as `pdf.<domain>`.
- Need to know which provider manages DNS records before giving console-specific record-creation steps.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed.

## Domain And DNS Provider Confirmed

Recorded: 2026-08-09 11:42:35 +08:00

### Background and Objective

The user identified `tuhepdf.cn` as the domain and Alibaba Cloud as the DNS-management provider. Selected a recommended canonical-hostname approach before creating DNS records.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched the prior DNS, domain, HTTPS, deployment, and `SITE_URL` entries.
- Preserved the decision not to replace `SITE_URL=http://localhost:8080` until the public hostname is deliberately chosen.

### Recommendation

- Use `tuhepdf.cn` as the canonical production URL because the registered domain is already product-specific.
- Add `www.tuhepdf.cn` as an additional DNS name, then redirect it to the canonical apex domain after the web proxy is configured.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed.

### Follow-up

- Await user confirmation of the canonical hostname, then create Alibaba Cloud DNS A records and verify propagation before HTTPS configuration.

## Canonical Hostname Approved And DNS Instructions Prepared

Recorded: 2026-08-09 11:55:01 +08:00

### Background and Objective

The user approved `tuhepdf.cn` as the canonical public hostname, with `www.tuhepdf.cn` as an additional hostname. Prepared Alibaba Cloud DNS instructions; the user must make the account-side DNS change.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched the prior DNS, domain, HTTPS, and `SITE_URL` entries.
- Preserved the local build configuration until DNS propagation and HTTPS setup are verified.

### Required DNS Records

- Public A record: host `@`, value = the ECS public IPv4 address.
- Public A record: host `www`, value = the same ECS public IPv4 address.
- Use the default DNS line and default TTL. Do not create a wildcard record and do not use the ECS private IP.

### Sources and Verification

- Consulted Alibaba Cloud's official public DNS record and beginner website-domain configuration guidance.
- DNS changes and propagation verification remain pending user action.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

## Production Deployment Deferred

Recorded: 2026-08-09 12:42:37 +08:00

### Background and Objective

After the server prerequisites were prepared, the user clarified that the current phase remains local secondary development and that public deployment must not proceed yet.

### Prior Log Entries Consulted and Decisions Preserved

- Preserved the local development baseline, including the existing `compose.yml` with `SITE_URL=http://localhost:8080`.
- Preserved the previously completed server readiness work: Docker, registry access, domain DNS, web ports, and Workbench access.

### Work Stopped

- A local Docker production-image build was requested but not authorized; it did not run.
- No image export archive was generated.
- No file was uploaded to the server, and no production container, reverse proxy, HTTPS certificate, or public application endpoint was started.

### Local Files Prepared

- Added `deploy/Caddyfile` and `deploy/compose.production.yml` as inactive future deployment templates.
- Added `deploy/*.tar` to `.gitignore` so a future exported image archive is not committed.
- The local development compose configuration was not changed.

### Follow-up

- Return to local secondary-development work. Resume production-image build and server deployment only after the user explicitly requests release of a local version.

## Direct SSH Validation Timed Out

Recorded: 2026-08-09 12:33:18 +08:00

### Background and Objective

The user tested a new direct SSH connection from Windows after removing the public SSH rule. The connection to TCP 22 timed out.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched Workbench, SSH, security-group, source-IP, and timeout entries.
- Preserved the requirement to avoid restoring public `0.0.0.0/0` SSH access.

### Diagnosis

- A TCP timeout means the source did not match an effective security-group rule or the rule was not applied to the target instance; it is not an authentication failure.
- The configured personal `/32` source is likely not the computer's current public egress address, which could not be verified by the prior IP lookup services.

### Recommended Recovery

- Use the Workbench prompt's one-click rule for `100.104.0.0/16` on TCP 22 to restore the Alibaba Cloud management channel.
- Keep the personal `/32` rule for later review, but do not relax SSH to all public addresses.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

## Workbench Access Rule Prompt

Recorded: 2026-08-09 12:31:09 +08:00

### Background and Objective

Reviewed the user's Alibaba Cloud Workbench security-group prompt after broad SSH/RDP rules were removed.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched Workbench, SSH, security-group, and public-IP entries.
- Preserved the intent to keep public web ports 80/443 open while avoiding a public `0.0.0.0/0` SSH rule.

### Finding

- Workbench reports that source range `100.104.0.0/16` needs inbound TCP 22 permission for the Workbench service to connect.
- The console's “one-click add” action can add this internal Workbench source rule without reopening SSH to all Internet addresses.

### Follow-up

- User can click “一键添加” to restore Workbench access, then continue using that session for deployment. The personal `/32` SSH rule can remain for direct SSH access.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

## Security Group Hardening Reported Complete

Recorded: 2026-08-09 12:27:55 +08:00

### Background and Objective

The user reported that a new restricted SSH(22) rule was added and the previous two risky rules were deleted. This section records the reported state before a new-session validation.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched security-group, SSH, RDP, and deployment entries.
- Preserved the requirement to keep the active administration session open until a new SSH connection is proven.

### Reported Result

- Restricted SSH rule added.
- The previous broad SSH and RDP rules were deleted.
- Web ports 80 and 443 remain publicly allowed.

### Follow-up

- Open a second SSH session from the user's computer to confirm the restricted rule works. After that, proceed to application image delivery and HTTPS reverse-proxy deployment.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

## Public IP Lookup Via HTTPS Failed

Recorded: 2026-08-09 12:21:33 +08:00

### Background and Objective

The TLS 1.2 `curl.exe` fallback to `api.ipify.org` also failed while trying to identify the administrator's public IP for a restricted SSH security-group rule.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched SSH, security-group, public-IP, and TLS entries.
- Preserved the requirement not to remove the current public SSH rule before a verified replacement rule exists.

### Diagnosis and Next Step

- The Windows network reset the external HTTPS connection. This does not affect ECS, Docker, DNS, or the deployed site path.
- Use a DNS-based public-IP query next, avoiding the failed HTTP/TLS route.
- If no verified administrator public IP can be determined, defer SSH restriction rather than risk locking out remote administration; still remove unused RDP exposure.

### Sources and Verification

- Consulted Alibaba Cloud official guidance confirming that SSH/RDP management ports should be restricted to specific source IPs.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

## Administrator Public IP Lookup Fallback

Recorded: 2026-08-09 12:18:45 +08:00

### Background and Objective

The user attempted to determine their current public IP before restricting SSH access, but the default Windows PowerShell request to `api.ipify.org` failed with a connection-closed error.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched prior SSH, security-group, public-IP, and TLS guidance.
- Preserved the safe security-group sequence: add the restricted SSH rule before deleting the existing public SSH rule.

### Diagnosis and Follow-up

- The failed request affects only the selected public-IP lookup service or the legacy PowerShell TLS connection; it does not indicate an ECS, DNS, or Docker problem.
- Retry with TLS 1.2 explicitly enabled, then use a browser-based public-IP lookup as a fallback.
- Do not share the administrator IP unnecessarily; use it locally as a `/32` security-group source value.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

## Security Group Review

Recorded: 2026-08-09 12:16:37 +08:00

### Background and Objective

Reviewed the user's ECS security-group screenshots after adding public web-service access and after the console warned about broad SSH and RDP access.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched security-group, SSH, port, HTTPS, and deployment entries.
- Preserved the public TCP 80 and 443 rules and the decision not to expose container port 8080.

### Findings

- Inbound TCP 80 and 443 are correctly allowed from the public Internet for the website and certificate validation.
- Existing TCP 22 is open to `0.0.0.0/0`; it should be replaced with a rule restricted to the administrator's current public IP address.
- Existing TCP 3389 is open to `0.0.0.0/0`; it is not needed for the Linux instance and should be removed.
- Public ICMP remains enabled; it is not required for the website and can be removed or restricted after access is confirmed.

### Safe Remediation Sequence

1. Add a new inbound TCP 22 rule for the administrator's public IPv4 address with a `/32` mask.
2. Test a second remote session while keeping the current session open.
3. Delete the old public TCP 22 rule, then delete public TCP 3389. Optionally remove public ICMP.

### Sources and Verification

- Compared the screenshots with Alibaba Cloud's official security-group guidance that public websites expose only TCP 80/443 and that management access should be restricted.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

### Follow-up

- After both records are enabled, verify public DNS resolution, then update production configuration and provision HTTPS.

## Alibaba Cloud Quick DNS Form Reviewed

Recorded: 2026-08-09 12:04:33 +08:00

### Background and Objective

Reviewed the user's Alibaba Cloud quick DNS-addition form screenshot to prevent incorrect hostname or IP selection before the user submits the public A records.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and the existing `tuhepdf.cn` DNS decision entries.
- Preserved `tuhepdf.cn` as canonical and `www.tuhepdf.cn` as the additional hostname.

### Form Review

- IPv4 resolution is selected and correct.
- The `tuhepdf.cn` checkbox is selected; the user must also select `www.tuhepdf.cn`.
- The website IP field must contain the ECS public IPv4 address only. The private IP and wildcard-host field must remain unused.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.

### Follow-up

- User submits the two A records, then runs DNS lookups from Windows to confirm both hostnames resolve to the ECS public IP.

## Public DNS Records Created

Recorded: 2026-08-09 12:06:04 +08:00

### Background and Objective

Reviewed the user's Alibaba Cloud confirmation screenshot after creating the public DNS records for the approved production hostnames.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and all relevant DNS/domain/HTTPS entries.
- Preserved `tuhepdf.cn` as canonical, `www.tuhepdf.cn` as an additional hostname, and the requirement to wait for public DNS verification before HTTPS.

### Result

- Alibaba Cloud confirmed two A records were created: `@` and `www`.
- Both records use the ECS public IPv4 address and the default 10-minute TTL.

### Files and Changes

- Changed only this task log; no application or server configuration was changed by the agent.

### Follow-up

- Verify DNS resolution from Windows after propagation, then configure the production application, reverse proxy, and HTTPS certificate.

## DNS Propagation Verified

Recorded: 2026-08-09 12:08:25 +08:00

### Background and Objective

Reviewed the user's Windows `nslookup` output after the Alibaba Cloud records were created.

### Prior Log Entries Consulted and Decisions Preserved

- Re-read the logging index and searched DNS, domain, HTTPS, and deployment entries.
- Preserved the canonical `tuhepdf.cn` hostname, `www` alias, and the requirement to keep `SITE_URL` local until production configuration is deployed.

### Verification Result

- `tuhepdf.cn` resolved to the ECS public IPv4 address.
- `www.tuhepdf.cn` resolved to the same ECS public IPv4 address.
- DNS propagation is confirmed from the user's current resolver.

### Next Infrastructure Requirement

- Before HTTPS and reverse-proxy deployment, allow inbound TCP ports 80 and 443 in the ECS security group. Keep management port 22 restricted and do not expose application port 8080 publicly.

### Files and Changes

- Changed only this task log; no application, DNS, or server configuration was changed by the agent.
