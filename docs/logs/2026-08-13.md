---
date: 2026-08-13
tags:
  - frontend
  - workbench
  - homepage
  - responsive
  - playwright
status: completed
---

# 首页快捷工具与布局调整

## 14:32 主界面精简与八个常用工具入口

### 背景与目标

根据主界面截图调整工作台首页：删除“按类别浏览”及三个折叠分类，重新安排搜索框、快捷卡片和本地处理提示的间距，并在原有六个常用工具前增加“图片转 PDF”和“PDF 转 PNG”。

本任务未查阅历史日志；依据 `docs/ROADMAP.md`、现有首页结构和 Design Token 实施，保留 iframe 工作台、深色/暖白主题与搜索回车打开工具的既有行为。

### 文件

- 修改 `index.html`：删除分类浏览区域；新增 `image-to-pdf`、`pdf-to-png` 快捷卡片，并保持八个入口的指定顺序。
- 修改 `src/css/styles.css`：首页内容宽度调整为 `820px`，搜索框调整为最大 `620px` 全宽主控件；快捷卡片使用桌面 4×2、移动端 2 列布局；删除分类区域的专用样式。
- 修改 `src/js/workbench.ts`：删除已无对应 DOM 的分类折叠搜索逻辑，保留八个快捷入口的过滤、空状态和 Enter 打开首个匹配工具。
- 修改 `scripts/phase2-visual-check.mjs`：新增快捷卡片数量、前两项顺序、分类区不存在、PNG 搜索结果和 Enter 导航的回归断言。
- 创建 `docs/logs/2026-08-13.md`，更新 `docs/logs/README.md`。
- 未移动或删除仓库文件；`public/wasm/` 和 `output/playwright/` 均为既有生成目录，不纳入本次提交文件。

### 命令与验证

- `npm install`：依赖为最新；`prepare` 执行 `husky`。命令输出完成后触及工具短超时，未产生 `package.json` 或 `package-lock.json` 变更。
- `npm run setup:wasm`：通过；PyMuPDF 15 个文件、Ghostscript 2 个文件、CoherentPDF 1 个文件均通过固定版本与 SHA-256 校验。
- `npm run build`：通过；包含翻译键、TypeScript、Vite、同源资源、多语言静态页、sitemap、安全响应头和 SEO 审计。
- `node scripts/phase2-visual-check.mjs`：通过；首页八个快捷入口、顺序、搜索导航、桌面布局和 375px 移动端布局通过，Phase 2 其余工具页视觉场景同时通过。
- 人工检查 `output/playwright/workbench-topbar.png` 与 `output/playwright/workbench-mobile-375.png`：桌面 4×2 和移动端 2 列布局无重叠、截断或横向溢出。
- `git diff --check`：通过，仅提示 Windows 工作区后续可能进行 LF/CRLF 转换。

### 警告、风险与后续

- 构建仍报告第三方 `wasm-vips` 的 direct `eval`、`embedpdf-snippet` 的 browser externalization 以及部分 chunk 超过 500 kB；均为既有依赖警告，本次首页修改未新增。
- 首次在沙箱内执行 Vite 构建和 Chromium 验证分别遇到 `spawn EPERM`；在获准的沙箱外环境重跑后均通过。
- 首页分类入口已按要求删除，完整工具发现能力继续由左侧工具栏及其搜索提供。

## 14:54 Phase 0、2、3 路线图实施审查

### 背景与目标

依据 `docs/ROADMAP.md` 审查 Phase 0、Phase 2、Phase 3 的当前实现，重点核对严重功能遗漏、门控真实性、主题运行时行为、Design Token 覆盖和测试缺口。本任务未查阅历史日志；仅检查路线图、相关源码、当前提交差异和运行时结果。

### 文件

- 未修改功能代码或配置。
- 修改 `docs/logs/2026-08-13.md`：追加本次审查记录。
- 修改 `docs/logs/README.md`：补充当日审查摘要与标签。
- Playwright CLI 生成的 3 个临时页面快照已删除；未移动或删除仓库业务文件。

### 审查结论

- Phase 0 核心门控通过：`isSameOriginPath` 反斜杠回归实现与测试存在，workflow 删除控件已改为本地化 `<button>`，WASM 固定版本与 SHA-256 准备成功，字体许可证和 iframe ADR 存在。
- Phase 2 核心门控通过：Design Token、公共 `.ui-*` 组件、思源黑体子集、迁移指南、工作台及 10 个高频工具视觉脚本存在；Chromium 桌面和 375px 场景通过。
- Phase 3 存在严重遗漏：主题切换不会同步已打开的 iframe；多组 `.ui-*` 颜色组合不满足 WCAG AA；`compare-pdfs.html`、`pdf-layers.html`、`pdf-workflow.html` 仍有大量主题相关硬编码颜色；`docs/zh-TW-review-checklist.md` 不存在且没有 4219 个 `zh-TW` 工具键的人工复核证据。
- `scripts/migrate-visual-batch.mjs --check` 因 `form-creator.ts` 的 4 个排除色值仍被最终扫描计错而退出 1，不能作为可靠的完成门控。
- GitNexus 对相对 `origin/main` 的 293 文件、739 符号变更评为 `CRITICAL` 范围风险，映射到 73 条执行流；`initTheme` 自身仅有 `src/js/main.ts:init` 一个直接调用者，修复主题同步的局部调用风险较低。

### 命令与验证

- `npm install`：通过，依赖已是最新。
- `npm run setup:wasm`：通过；PyMuPDF 15 个、Ghostscript 2 个、CoherentPDF 1 个文件完成 hash 校验。
- `npm run test:run`：通过，36 个测试文件、800/800 用例；jsdom 输出 2 条未实现 `HTMLCanvasElement.getContext()` 的非失败提示。
- `npm run check:translations`：通过；`zh`、`zh-TW` 均为 756/756 `common` 和 4219/4219 `tools` 本地键，但这只验证键完整，不代表人工润色完成。
- `npm run build`：通过；翻译、TypeScript、Vite、dist 本地资源、静态多语言页、sitemap、安全响应头和 SEO 审计成功。
- `node scripts/phase2-visual-check.mjs`：通过，生成并验证 32 个 Phase 2 截图场景；脚本没有 Phase 3 深浅主题或 iframe 同步断言。
- Playwright CLI 实测：打开 `merge-pdf` 后切换主题，父页面为 `data-theme="light"`、背景 `rgb(245, 243, 236)`，iframe 仍为 `data-theme="dark"`、背景 `rgb(23, 26, 24)`。
- 对比度计算：主按钮深色/暖白分别约 `2.97:1`、`2.76:1`；暖白成功/警告/危险/信息状态约 `2.34:1`、`1.83:1`、`3.12:1`、`1.82:1`，均低于普通文字 WCAG AA 的 `4.5:1`。
- `git diff --check`：通过，仅有 Windows LF/CRLF 提示。

### 警告、风险与后续

- Phase 3 当前不应标记完成，也不满足进入生产部署 Phase 1 的路线图门控。
- 应先修复主题在现有 iframe 中的即时同步，并为深色/暖白状态增加 Playwright 断言；同时增加独立的按钮前景色和暖白状态色 token，做自动对比度门控。
- 应将 3 个保留内联主题颜色的复杂工具页人工迁移，并扩展扫描器识别 CSS 十六进制与 `rgba()` 颜色，避免只扫描 Tailwind class 的假阴性。
- `zh-TW` 人工润色需要可审计清单和具备繁体中文能力的人员逐页确认，不能用翻译键完整性检查替代。

## 15:18 Phase 3 审查问题的低回归修复方案

### 背景与目标

针对前一轮审查发现的主题 iframe 不同步、WCAG AA 不达标、复杂工具页硬编码颜色、`zh-TW` 人工复核缺失和迁移门控误报，制定可分批实施、独立验证和逐步回滚的解决方案。本任务未查阅历史日志；仅基于当前源码、`docs/ROADMAP.md` 和 GitNexus 调用关系进行影响分析，不修改功能代码。

### 影响分析与决策

- GitNexus 显示 `initTheme` 为 LOW 风险，只有 `src/js/main.ts:init` 一个直接调用者；`initWorkbench` 也为 LOW 风险且仅由 `main.ts:init` 调用。主题同步应只修改 `src/js/utils/theme.ts`，不进入 600 多行的工作台标签页流程。
- 将主题应用与持久化分离：初始化和 `storage` 事件只应用主题，用户点击才写 `localStorage`，避免 iframe 写回循环；保留现有 `tuhe.theme` 作为 canonical key，或在必须改名时采用双读单写迁移，不能直接丢弃既有偏好。
- 不直接改写共享 `--color-success`、`--color-warning`、`--color-danger`、`--color-info`；新增 `--color-on-action`、`--color-on-danger` 和 `--color-status-*-text`，仅让按钮与文字组件消费对应 token，避免影响进度条、图例和 PDF 内容颜色。
- `compare-pdfs.html`、`pdf-workflow.html` 需区分主题 UI、PDF 纸张/差异叠层和工作流类别色；固定业务颜色转为命名的页面级 custom properties 并记录豁免，只有 UI chrome 接入全局主题。`pdf-layers.html` 的颜色均按 UI chrome 迁移。
- `scripts/migrate-visual-batch.mjs` 的 Windows 路径需先标准化为 `/`；当前 `semanticColorExclude` 使用 POSIX 路径而 `path.relative` 返回反斜杠，是排除逻辑失效和 4 个误报的根因。最终门控应支持带理由的精确豁免，并扫描内联 CSS，不再只匹配 Tailwind class。
- `zh-TW` 采用工具/命名空间小批次人工复核；OpenCC 只生成候选 diff，不自动覆盖已审文案。清单记录实际 4219 个键的批次、审阅人、日期和状态，并增加占位符、HTML 标记与键集合一致性检查。

### 建议实施顺序与验证

- 提交 1：主题模块和单元测试；验证初始化、点击持久化、无效值回退、`storage` 同步不写回。
- 提交 2：Playwright Phase 3 主题场景；验证已打开 iframe 即时同步、新开 iframe 继承、刷新持久化和直接打开工具页。
- 提交 3：用途型颜色 token 和自动对比度检查；普通文字门槛 `4.5:1`，非文字控件边界门槛 `3:1`。
- 提交 4–6：分别迁移 `pdf-layers`、`pdf-workflow`、`compare-pdfs`，每页保留迁移前后双主题截图并运行对应工具主流程。
- 提交 7：修复迁移检查器并增加 Windows/Linux 路径测试、内联 CSS 扫描和精确豁免清单。
- 提交 8 起：按工具分批完成人工 `zh-TW` 复核；每批运行翻译检查和代表性 i18n E2E。
- 每个提交均运行聚焦测试；最终运行 `npm install`、`npm run setup:wasm`、`npm run test:run`、`npm run build`、Phase 2 视觉套件、新增 Phase 3 主题/对比度套件、迁移门控和 `git diff --check`。

### 文件、命令、风险与后续

- 修改 `docs/logs/2026-08-13.md`，未创建、移动或删除其他文件；`docs/logs/README.md` 的当日摘要已涵盖 Phase 3 审查，无需再次修改。
- 执行 GitNexus `list_repos`、`query`、`impact`、`context` 和源码 `rg`；全文查询提示 FTS 缺失，但结构化符号影响结果可用，并已用源码交叉确认。
- 绝对零回归无法由设计承诺保证；上述方案通过小提交、用途隔离、真实 iframe E2E、计算对比度门控和逐页截图把风险限制在可检测、可回滚范围。

## 15:46 Phase 3 修复结果复审

### 背景与目标

复审用户提供的 5 项修复摘要与当前未提交工作树，确认实现是否真的通过验证、是否存在脚本通过但运行时仍回归的情况。本任务未查阅历史日志；检查了当前差异、源码、测试脚本和本地命令结果。

### 审查结论

- iframe 主题同步实现方向正确：`applyTheme` 不写存储，点击是唯一写入口，iframe 监听 `storage`。但新增 `phase2-visual-check.mjs` 的 `validateThemeSync` 当前实际失败，`waitForSelector` 后立即从 `page.frames()` 查找 frame，存在加载竞态；应改为等待 iframe `load` / `contentFrame()` 就绪后再检查。
- 对比度 token 数值检查通过，但约 110 个页面的主按钮仍同时包含 `.ui-button-primary` 与 `.ui-text-primary`；后定义的 `.ui-text-primary`（`styles.css` 2158 行附近）会覆盖按钮规则的 `--color-on-action`。因此脚本的 14 项 token 对通过，不等于实际按钮都满足 WCAG AA。
- `check-contrast`、`migrate-visual-batch --check` 和 `check-zh-tw-review` 均未接入 `npm run build`；后续构建不会自动执行这些 Phase 3 门控。
- `parse5` 与 `postcss` 仅作为 `jsdom` / `vite` 的传递依赖出现，未声明为项目直接依赖；迁移脚本依赖传递依赖的扁平化结果，干净安装或依赖升级可能失效。
- zh-TW 门控按预期失败：0/4219 键已复核，123 个命名空间未完成，另有 18 个占位符问题和 1 个快捷键问题。`SHORTCUT_TOKENS` 目前不包含注释中声称覆盖的 `Enter`、`Shift`、`Tab`、`Alt` 等 token；manifest 只保存命名空间，不机器校验清单中的复核人、日期和键数。

### 命令与验证

- `npm install`：通过。
- `npm run setup:wasm`：通过，固定 WASM 资源 hash 校验成功。
- `npm run check:contrast`：通过，14 项检查全部通过；未覆盖实际 CSS cascade。
- `node scripts/migrate-visual-batch.mjs --check`：通过，473 个文件、0 个待迁移、0 个未批准颜色。
- `node scripts/check-zh-tw-review.mjs`：按预期退出 1，报告 0/4219 已复核、123 个未复核命名空间和 19 个审计问题。
- `node scripts/phase2-visual-check.mjs`：当前工作树退出 1；33 张截图生成，但 `validateThemeSync` 报 `tool iframe did not become a frame`。
- `npm run test:run`：本轮沙箱外重跑申请未获自动批准，未将用户提供的 821/821 作为独立验证结果；上一轮基线 800/800 已知通过。

### 风险与后续

- 当前建议：主题同步先修复视觉脚本竞态；删除批量页面上的 `.ui-text-primary` / `.ui-text-danger` 覆盖，或提供更高优先级且用途明确的按钮规则，再用浏览器 computed style 做对比度断言。
- 将 `parse5`、`postcss` 加入直接 devDependencies；新增 `check:phase3` 聚合命令，手工复核完成前不要接入默认生产 `build`，但发布门控必须执行它。
- 扩充快捷键 token 校验并让 manifest 与 checklist 的状态、人、日期、键数互相校验。zh-TW 人工复核未完成前，Phase 3 仍不能标记完成。

## 17:16 Phase 3 审查遗留项修复

### 背景与目标

根据复审发现修复实际 CSS 级联、iframe 视觉测试竞态、门控未接线、脚本依赖未直接声明和 zh-TW 审计元数据不足等问题，目标是在不改变主题持久化行为和未完成人工翻译结论的前提下，使日常构建能阻止结构回归、发布门控能阻止未复核版本发布。本任务未查阅其他历史日志；沿用本文件上一条审查结论。

### 修改与决策

- `src/css/styles.css`：在兼容层末尾恢复启用态 `.ui-button-primary` / `.ui-button-danger` 的用途型前景色优先级，避免后定义的文字和 hover utility 覆盖 WCAG token；禁用态仍由原高优先级规则控制。
- `scripts/phase2-visual-check.mjs`：等待 iframe `contentDocument` 与 `contentFrame()` 就绪，消除 `page.frames()` 导航竞态；新增主按钮和 `digital-sign-pdf` 危险按钮在深浅主题下的 computed style 断言。
- `scripts/check-zh-tw-review.mjs`、`docs/zh-TW-review-manifest.json`、`docs/zh-TW-review-checklist.md`：拆分日常结构门控与严格人工复核门控；校验命名空间覆盖、键数、复核人、真实日期、`lastReviewed` 和 manifest/checklist 一致性；扩充组合快捷键识别，对中文无需呈现的英语复数后缀使用按键、按占位符的窄范围豁免。
- `public/locales/zh-TW/tools.json`：恢复 11 条错误信息的运行时详情占位符，并修正 `pdfBooklet.pageSheetSummary` 串键及箭头缺失；英语复数后缀和已由繁中名词表达的工具名未强行插回文案。
- `package.json`、`package-lock.json`：将既有锁定版本 `parse5@8.0.0`、`postcss@8.5.16` 声明为直接 devDependencies；新增 `check:phase3`、`check:phase3:release` 等脚本，默认 `build` 执行结构门控，严格发布门控在人工复核后继续构建和浏览器检查。

### 命令与验证

- `npm install --save-dev --save-exact parse5@8.0.0 postcss@8.5.16`：通过，`npm ls --depth=0` 确认两项均为直接依赖。
- `npm run setup:wasm`：通过，PyMuPDF、Ghostscript、CoherentPDF 固定资源 hash 校验成功。
- `npm run check:phase3`：通过；对比度 14/14，视觉迁移 473 个文件、0 待迁移、0 未批准颜色，zh-TW 结构门控通过。
- `npm run test:run`：沙箱内因 Windows `spawn EPERM` 无法启动，获准沙箱外重跑后 38 个文件、821/821 用例通过；仅有 jsdom 未实现 Canvas 的既有提示。
- `npx tsc --noEmit`、`npm run build`：通过；构建入口已实际执行新 Phase 3 结构门控并完成 2562 个 HTML、静态资源和 SEO 审计。保留依赖包 `wasm-vips` 的 direct eval、chunk size 等既有警告。
- `npm run check:phase2-visual`：最终重跑通过，33 张截图，主题 iframe 同步、独立页继承、主按钮和危险按钮计算前景色断言均通过。
- `node scripts/check-zh-tw-review.mjs --structure-only`、目标文件 `npx prettier --check`、`git diff --check`：通过；严格 `node scripts/check-zh-tw-review.mjs` 按预期退出 1，仅报告 123 个命名空间、4219 键尚未人工复核。
- GitNexus `detect_changes`：LOW 风险，未发现受影响执行流程；新增脚本函数尚未进入旧索引。

### 已知警告、风险与后续

- zh-TW 人工复核仍为 0/4219，本次只修复可机械确认的结构问题；`check:phase3:release` 会继续阻止发布，直到 123 个命名空间由具备繁中能力的人员逐批登记完成。
- 曾误调用包含 `prettier --write .` 的仓库 `format` 脚本；它在第三方 PDF.js HTML 报错前改写了多文件格式。已依据执行前记录的 8 文件白名单恢复所有额外内容差异并刷新 Git 索引，最终工作区仅保留上述目标文件和本日志/索引。
- 未提交任何文件，未修改业务功能函数；本次生成的 Playwright 截图位于未跟踪输出目录且未纳入提交。
