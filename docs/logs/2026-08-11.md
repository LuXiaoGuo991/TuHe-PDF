---
date: 2026-08-11
tags:
  [review, i18n, branding, seo, testing, wasm, ocr, offline, security, delivery]
status: completed
---

# 最近中文化与品牌提交 Review（2026-08-11 12:15 +08:00）

## 背景与目标

审查 `8c6f02c`、`2be77c1`、`58e9090` 三次提交，评估 TuHe PDF 的中文化、国际化和品牌重命名是否保留正确行为，并识别遗漏。

## 历史日志与决策

未查阅历史任务日志；仅更新 `docs/logs/README.md` 的当日索引。保留代码 review 结论，不直接修改被审查的功能代码。

## 文件变更

- 新增 `docs/logs/2026-08-11.md`。
- 修改 `docs/logs/README.md`，增加当日索引项。

## 执行命令与验证

- `git log -8 --oneline --decorate --stat`：确认最近三次目标提交。
- `git diff --check HEAD~3 HEAD`：通过，无空白错误。
- `npm run build`：通过；首次受沙箱 `spawn EPERM` 影响，授权后成功完成 TypeScript、Vite、i18n 页面、sitemap、安全头和 SEO 审计。
- `node scripts/check-translations.js`：失败；除 `en`、`zh` 外的 19 个语言包均缺少 155 个 `common` 键，含 `zh-TW`。
- `npm run test:run`：843 项中 840 项通过、3 项失败。`src/tests/helpers.test.ts` 的 `Custom` 期望未随 `helpers.custom` 本地化更新；另有两个既存 qpdf 测试 fixture 缺失。
- 使用 `Sun, 8 Jan 2017 20:37:44 +0200` 验证 `formatRawDate`：当前输出为 `2017年1月9日星期一 2:37 (UTC+02:00)`，显示时间与标注时区不一致。

## Review 结论与风险

- `scripts/generate-i18n-pages.mjs`、`scripts/generate-sitemap.mjs`、`scripts/seo-audit.mjs` 及 `public/robots.txt` 仍默认 `https://tuhe-pdf.com`，但页面源文件已使用 `https://www.tuhepdf.cn`。构建产物的 canonical、`hreflang`、sitemap 和结构化数据会混用两个域名。
- `src/js/utils/helpers.ts` 的 `formatRawDate` 改为 `Date` 本地时区格式化后又附加原始时区，邮件转 PDF 会写入错误的发送时间。
- `src/js/i18n/i18n.ts` 配置 `fallbackLng: 'en'`，而繁体中文等 19 个语言包缺键，将回退英文；品牌名称 `Bentopdf` 仍出现在多个非中文语言包的用户可见内容。
- `cloudflare/wasm-wrangler.toml` 示例使用 `@tuhe-pdf/*`，与实际默认值、环境示例和 `cloudflare/WASM-PROXY.md` 的 `@bentopdf/*` 不一致。

# Review 问题修正方案（2026-08-11 12:20 +08:00）

## 背景与目标

根据上一条 review 结果，为域名迁移、邮件时间格式化、翻译键完整性、品牌残留和测试门禁制定按优先级执行的修正方案；本次仅提供方案，不修改功能代码。

## 历史日志与决策

未新增查阅历史任务日志；沿用本文件上一条 review 结论。修正顺序采用先修复用户可见错误，再统一配置，最后接入自动化门禁。

## 文件变更

- 仅追加本日志条目，未修改功能代码、配置或测试文件。

## 执行命令和验证结果

- 本轮未执行代码命令；方案中的验证命令待实施时执行。

## 已知风险和后续工作

- 域名最终以 `https://www.tuhepdf.cn` 还是 apex 域名为准，需要部署方确认一个规范主域；方案默认使用页面当前声明的 `https://www.tuhepdf.cn`。
- `@bentopdf/*` 是实际现有 WASM 包名，不能仅因品牌改名而改成未发布的 `@tuhe-pdf/*`。

# 中文化、品牌与测试问题修复（2026-08-11 15:14 +08:00）

## 背景与目标

按上一条方案顺序修复规范域名分裂、邮件原始时间显示、翻译键回退、品牌残留、WASM 示例地址和失败测试，并将翻译键可解析性接入构建门禁。

## 历史日志与决策

未额外查阅历史任务日志；沿用本文件两条既有 review 与方案的结论。规范站点地址统一为 `https://www.tuhepdf.cn`；繁体中文按 `zh-TW -> zh -> en` 回退，其他非英文语言按 `en` 回退，避免伪造机器翻译内容。

## 文件变更

- 新增 `scripts/site-config.mjs`、`src/js/i18n/fallback-languages.js`，供 SEO 生成器与 i18next 共享配置。
- 修改 `.env.example`、`Dockerfile`、`public/robots.txt`、`scripts/generate-i18n-pages.mjs`、`scripts/generate-sitemap.mjs`、`scripts/seo-audit.mjs`、`src/js/utils/markdown-editor.ts`，统一使用 `https://www.tuhepdf.cn`。
- 修改 `src/js/utils/helpers.ts` 与 `src/tests/helpers.test.ts`，保持 RFC 2822 原始墙上时间及偏移量，并覆盖未识别输入与中文默认值。
- 修改 `scripts/check-translations.js`、`package.json`、`src/js/i18n/i18n.ts`，以实际 i18next 回退链检查 `common` 和 `tools` 的全部键，并在 `npm run build` 前运行检查。
- 修改多份 `public/locales/*/common.json` 和 `public/locales/zh/tools.json`，清除用户可见 `Bentopdf` 文案及过期键。
- 修改 `cloudflare/wasm-wrangler.toml`、`cloudflare/WASM-PROXY.md`，示例恢复为实际发布的 `@bentopdf/pymupdf-wasm@0.11.16`、`@bentopdf/gs-wasm@0.1.1` 地址。
- 修改 `src/tests/split-pdf-helpers.test.ts`，以测试内构造的共享资源 PDF 和带目录 PDF 替代未提交的二进制 fixture。

## 执行命令和验证结果

- `npm run test:run -- src/tests/split-pdf-helpers.test.ts`：通过，40 项。
- `npm run test:run`：通过，40 个测试文件、845 项断言；仅保留 jsdom 缺少 Canvas 实现的提示。
- `npm run build`：通过，包含翻译检查、TypeScript、Vite、语言页、sitemap、安全头和 SEO 审计；构建产物的 canonical、hreflang、robots 与 sitemap 均为 `https://www.tuhepdf.cn`。
- `npx eslint ...`（本次修改的 JS/TS 文件范围）：通过，无错误。
- `git diff --check`：通过，无空白错误；仅有 Git 自动换行提示。
- `rg -n -g '!docs/logs/**' -g '!dist/**' 'tuhe-pdf\\.com|@tuhe-pdf/' .`：无匹配。
- `npm run lint`：未通过，失败集中于未修改的 `public/wasm/**` 第三方生成文件，共 127 个错误、5429 个警告；未在本任务中改动第三方产物。

## 已知风险和后续工作

- 本次保证所有翻译键都可解析，不等同于补齐人工译文。`zh-TW` 仍有 155 个 `common` 键和 2094 个 `tools` 键显示简体中文回退；其他语言的同类缺键显示英文回退，应由语言人员分批补译。
- 生产构建仍输出 `wasm-vips` 的 direct `eval`、浏览器外部化 Node 模块和大 chunk 警告，均来自既有依赖或打包体积，未阻塞构建；上线前应单独评估依赖安全与分包策略。

# 二次开发最终检查与中文用户体验评估（2026-08-11 15:58 +08:00）

## 背景与目标

对近期品牌重塑、中文国际化、域名与 WASM 配置改动做最终只读检查，评估项目二次开发完成度及中国大陆用户使用友好度。

## 历史日志与决策

未查阅历史任务条目；仅读取当日文件尾部以追加本条。沿用当前 `TuHe PDF` 品牌、`https://www.tuhepdf.cn` 规范域名和 `zh-TW -> zh -> en` 回退策略，不修改功能代码。

## 文件变更

- 未修改功能代码、配置或测试文件。
- 生成并随后删除本轮浏览器检查的临时截图；预览进程与浏览器进程已停止，保留用户原有的旧预览服务。

## 执行命令和验证结果

- `git status --short --branch --untracked-files=all`：检查前后均为干净工作区，`main` 比 `origin/main` 超前 3 个提交。
- `npm run check:translations`：通过；简体中文 `common` 421/421、`tools` 2974/2974，所有键均可按回退链解析。
- `node scripts/i18n-inventory-html.mjs`：通过，0 处未接线静态 HTML 文案。
- `node scripts/i18n-audit-ts.mjs`：通过，0 处疑似硬编码动态文案。
- `npm run build`：通过；包含翻译检查、TypeScript、Vite、多语言静态页、sitemap、安全头与 SEO 审计。
- `npm run test:run`：通过，40 个测试文件、845 项测试；仅有 jsdom 未安装 Canvas 实现的提示。
- `npm run lint`：未通过，127 个错误、5428 个警告，主要集中于既有 `public/wasm/**` 压缩/生成产物。
- `node node_modules/eslint/bin/eslint.js src scripts cloudflare --max-warnings=0`：0 个错误、497 个警告；因零警告门槛退出 1，警告主要是既有安全规则提示、全局类型和未使用变量。
- Playwright 首页检查：`/zh/` 返回 200，标题、导航、常用工具、搜索提示、语言菜单均为中文；控制台仅有开发模式/Locize/分享提示，无异常。
- HTTP 检查 `/zh/merge-pdf.html`：返回 200，中文标题与正文翻译键存在；构建 HTML 仍保留英文 fallback 文本，运行时需依赖 i18n 替换。

## 已知警告、失败、风险和后续工作

- 高优先级：`src/js/utils/wasm-provider.ts` 默认把 PyMuPDF、Ghostscript、CoherentPDF 指向 `cdn.jsdelivr.net`；`public/sw.js` 的 CDN 本地回退仅映射 LibreOffice，未映射这些高级 WASM。中国大陆网络受限时，压缩、PDF/A、书签/元数据及部分格式转换可能失败或长时间等待。应将生产默认地址切到 `wasm.tuhepdf.cn` 或站内 `/wasm/...`，并补齐 Service Worker 回退映射。
- 中优先级：OCR 中文字体默认来自 `rawcdn.githack.com`，`.env.example` 中 `VITE_OCR_FONT_BASE_URL` 为空；中文 OCR 导出在外部字体不可达时只能回退或失败。建议将简体中文字体随部署包提供。
- 中优先级：`src/partials/footer.html` 的 `Advanced Settings` 未接 `data-i18n`，中文页脚仍会显示英文；顶部 `Docs` 也保留英文标签。
- 中优先级：多语言工具页构建产物的可见正文初始仍是英文并带 `data-i18n`，需等待客户端初始化才替换；慢网、脚本异常或 SEO 抓取场景仍可能看到英文。建议构建期直接渲染 `zh` 正文。
- 低优先级：页脚仍有 GitHub、Discord、Instagram、LinkedIn、X 等外链，面向中国用户的支持渠道和可达性不足。
- 构建警告包括 `wasm-vips` direct `eval`、浏览器外部化 Node 模块和大 chunk；本轮未修改依赖，建议单独治理。

# 四项主要风险解决方案汇报（2026-08-11 16:12 +08:00）

## 背景与目标

根据上一条最终检查结果，整理高级 WASM 外部依赖、OCR 字体外部依赖、工具页初始英文和未本地化控件四项风险的对应解决方案与验收标准。

## 历史日志与决策

未查阅历史任务条目；沿用当日最终检查中的风险分级。当前仅输出方案，不修改功能代码。

## 文件变更

- 未修改功能代码、配置或测试文件。
- 追加本方案汇报日志。

## 方案摘要

- 高级 WASM：生产默认切换到 `wasm.tuhepdf.cn` 或站内 `/wasm/...`，为 PyMuPDF、Ghostscript、CoherentPDF 增加 Service Worker 本地回退，并在 CI 检查关键资源可达性。
- OCR 中文字体：将简体中文字体随部署包或国内对象存储发布，设置 `VITE_OCR_FONT_BASE_URL`，保留 IndexedDB 缓存和英文回退。
- 工具页初始英文：在构建期按语言包替换 HTML 的 `data-i18n` 文本，同时保留运行时 i18next 作为动态文案兜底。
- 未本地化控件：为 `Advanced Settings`、`Docs` 和后续新增控件补齐稳定 i18n 键，并通过 HTML 键审计和浏览器冒烟测试验收。

## 验收标准

- 断开或阻断 `cdn.jsdelivr.net`、`rawcdn.githack.com` 后，核心高级工具和中文 OCR 仍能加载或给出本地化、可操作的失败提示。
- `/zh/*.html` 在未执行 JavaScript 时正文已是中文；运行时切换语言后仍能正确更新动态文本。
- 中文首页、工具页、设置页和页脚不再出现可见英文控件文案；`npm run check:translations`、HTML/TS 审计、构建和测试全部通过。

# 四项风险实施与验收（2026-08-11 17:15 +08:00）

## 背景与目标

执行高级 WASM 外部 CDN、简体中文 OCR 字体、`/zh/*.html` 初始英文和顶部/页脚英文控件四项风险的修复。目标是在阻断 `cdn.jsdelivr.net` 与 `rawcdn.githack.com` 时仍从同源资源加载，并让中文静态页在未运行 JavaScript 时显示中文正文。

## 查阅历史日志与决策

- 查阅 `docs/logs/2026-08-11.md` 的当日最终检查和方案汇报条目，保留其 P1/P2 优先级与“同源 `/wasm/...`”方案。
- 生产默认统一使用 `/wasm/pymupdf/`、`/wasm/ghostscript/`、`/wasm/cpdf/`；保留已配置的 jsDelivr 地址兼容性，并由 Service Worker 在网络失败或非 2xx 响应时回退同源资源。

## 创建与修改的文件

- 新增 `public/wasm/ocr/fonts/NotoSansCJKsc-Regular.otf`（16,437,364 bytes）和 `public/wasm/ocr/fonts/NotoSans-Regular.ttf`（569,208 bytes），并在 `.gitignore` 精确放行，随部署产物发布。
- 修改 `.env.example`、`src/js/utils/wasm-provider.ts`、`src/js/utils/font-loader.ts`、`src/js/config/font-mappings.ts`、`src/pages/wasm-settings.html`、`scripts/generate-security-headers.mjs`、`security-headers-docs.conf`、`public/sw.js` 和 `package.json`，使默认资源、设置页建议地址和 CSP 均使用同源资源。
- 新增 `scripts/check-local-resources.mjs` 与 `scripts/check-i18n-static-pages.mjs`；前者检查 PyMuPDF、Ghostscript、CoherentPDF、Tesseract、中文语言数据和字体，后者逐页核对 `dist/zh` 的 `data-i18n`、placeholder、title。
- 修改 `scripts/generate-i18n-pages.mjs`，构建期解析 `common`/`tools` 命名空间及回退链并渲染静态文本。
- 修改 `index.html`、`src/partials/footer.html`、`src/partials/footer-simple.html`、`src/js/i18n/language-switcher.ts` 和中英文 `common.json`，补齐 `nav.docs`、`footer.advancedSettings`，并使用 `#footer-social-column`、`#simple-mode-lang-switcher` 等稳定 DOM 标识。
- 修改 `src/tests/font-loader.test.ts`、`src/tests/xss-replay.test.ts`，覆盖中文本地字体、同源 WASM 路径和协议相对 URL 拒绝。

## 命令与验证结果

- 官方 GitHub 直连下载 Noto 字体在 120 秒超时；按 `AGENTS.md` 使用 `https://v4.gh-proxy.org/` 公开 GitHub 下载兜底后成功下载两份字体，未传递凭据或私有地址。
- `npm run check:translations`：通过。
- `npx tsc --noEmit`：通过。
- `npm run test:run`：40 个测试文件、846 项测试全部通过；仅有既有 jsdom `HTMLCanvasElement.getContext` 提示。
- `npm run build`：通过，包含 `check-local-resources --dist`、124 个中文静态页审计、sitemap、安全头和 SEO 审计。
- Playwright 本地预览：阻断 JavaScript 后 `/zh/compress-pdf.html` 正文保持中文；`/zh/` 顶部 Docs 显示“文档”。手动注册 Service Worker 并阻断 `cdn.jsdelivr.net`、`rawcdn.githack.com` 后，PyMuPDF、Ghostscript、CoherentPDF 的历史 CDN 请求均由同源回退返回 HTTP 200，简体中文字体同源请求返回 HTTP 200（16,437,364 bytes）。

## 已知警告、风险与后续工作

- `npm run build` 仍输出既有 `wasm-vips` direct `eval`、`embedpdf-snippet` 的浏览器外部化 `crypto` 和大 chunk 警告；本任务未修改第三方依赖或分包策略。
- 浏览器验收覆盖资源加载与 Service Worker 回退，未使用上传文件执行一次完整的 PDF 转换；后续可在 CI 中加入带 PDF fixture 的端到端转换用例。

# 四项风险治理提交前审查（2026-08-11 17:43 +08:00）

## 背景与目标

审查四项风险治理的全部未提交改动，判断是否已满足提交条件。审查范围包括同源 WASM、OCR 字体、构建期中文渲染、稳定 i18n/DOM 标识及新增验证门禁；本任务只给出审查结论，不修改功能代码。

## 查阅历史日志与决策

- 未检索历史日期日志；仅读取 `docs/logs/2026-08-11.md` 尾部以按规范追加本条，并依据用户提供的本轮实施与验收摘要复核工作区。
- 保留全部现有业务改动，不代替用户修复或暂存；结论为当前不满足提交条件。

## 创建与修改的文件

- 仅追加 `docs/logs/2026-08-11.md` 并更新 `docs/logs/README.md` 当日索引摘要。
- Playwright 复核生成的 `page-2026-08-11T09-30-19-397Z.yml` 与 `console-2026-08-11T09-30-19-161Z.log` 已删除；未删除用户原有的 3 份未跟踪页面快照。

## 命令与验证结果

- `git diff --check`：通过；仅有工作区 LF/CRLF 转换警告。
- `npm run check:translations`：通过；简体中文 `common` 422/422、`tools` 2974/2974。
- `npm run check:local-resources`：通过，但仅证明当前工作区的忽略文件存在，不能证明干净检出可复现。
- `npm run check:i18n-static`：通过，覆盖 124 页；检查仅比较 `textContent`，未检查被删除的子元素。
- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- `npm run test:run`：沙箱内因 Vite 执行 `net use` 时 `spawn EPERM` 未启动；两次沙箱外重试均因审批检查超时未取得结果。本条未能独立复现用户报告的 846 项通过。
- Node URL 归一化验证：`/\\\\evil.example/x` 相对 `https://app.example` 解析为 `https://evil.example/x`，证明当前单斜杠判定可被反斜杠绕过。
- JSDOM 扫描发现 9 个 `[data-i18n]` 元素含子元素；真实浏览器确认 `/zh/pdf-booklet.html` 的 `#download-btn` 图标数为 0，构建产物已删除下载图标。
- `git ls-files public/wasm public/libreoffice-wasm` 无输出，`npm ls @bentopdf/pymupdf-wasm @bentopdf/gs-wasm coherentpdf --depth=0` 为空；高级 WASM 仅存在于被忽略的本地目录。
- 查阅 WHATWG Fetch Standard 的 Main fetch 顺序，确认 CSP 在 Service Worker 处理前阻断请求；仅用网络阻断验证 CDN 回退不能代表生产 CSP 场景。

## 已知警告、失败、风险和后续工作

- P1：`public/wasm/pymupdf`、`ghostscript`、`cpdf`、`tesseract` 被 `.gitignore` 排除且没有依赖安装/构建步骤，干净检出和远端 Docker 构建会缺少新增健康检查要求的资源。应提交可追溯资源或新增锁版本、校验和明确的准备步骤，并在干净检出中构建。
- P1：`isSameOriginPath()` 只拒绝 `//`，浏览器会把 `/\\host/` 归一化为跨域 URL。应基于 `new URL(value, location.origin)` 比较 `origin` 并拒绝反斜杠，增加回归测试。
- P1：保留的历史 jsDelivr 配置与移除 jsDelivr 的生产 CSP 冲突；CSP 会在 Service Worker 回退前阻断请求。应将精确的历史默认值迁移到同源值，或在迁移期保留 CSP 并使用真实生产头验收。
- P1：构建期和运行时翻译均直接覆盖含子元素节点的 `textContent`，已删除按钮下载图标、FAQ 箭头和强调标记。应把翻译键移到专用文本子节点或实现保留子元素的翻译策略，并增加 DOM 完整性审计。
- P2：`VITE_OCR_FONT_BASE_URL=/wasm/ocr/fonts/` 会把所有 OCR 字体指向本地目录，但当前只部署简体中文和 Noto Sans 两个文件，日文、繁中、韩文、阿拉伯文等映射会返回缺失资源；应取消“部分目录”的全局 base 配置或部署并审计全部声明字体。
- P2：两份 Noto 字体未附带可检索的 OFL/第三方许可证与来源校验记录，应在提交前补齐许可证、来源版本和校验和。
- 提交卫生：`.playwright-cli/page-*.yml` 仍有 3 个未跟踪文件；提交前应明确排除或加入忽略规则，避免 `git add .` 带入浏览器快照。
